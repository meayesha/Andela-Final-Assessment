# Prompt

Using the plan in **`instructions/gameplan.md`**, address the requirements in **`problem_statement.md`**. Follow the repo’s actual layout (`backend/`, `frontend/`, root `.env`, OpenRouter, SQLite, optional Clerk) and **`README.md`** for run and deploy steps—not the legacy “single root `api/index.py`” pattern unless you are explicitly migrating.

Reference **`instructions/deployment_knowledge_base/how_to_deploy_mcp_servers_to_vercel.md` section A** for Vercel vs Python API split, and the MCP/agents guides under **`instructions/mcp_knowledge_base/`** and **`instructions/agents_knowledge_base/`** as needed.
